import { useNavigate } from "react-router-dom";

export const Album = () => {
    const navigate = useNavigate();

    const handleButtonClick = () => {
        // navigate("/input")
    }
    
    return (
        <div>
            Album
        </div>
    
    )
}